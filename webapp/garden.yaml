kind: Module
type: container
name: webapp
dockerfile: Dockerfile
services:
  - name: webapp
    ingresses:
      - hostname: webapp.localhost
        path: /
        port: http
    ports:
      - name: http
        containerPort: 80
    volumes:
      - name: data
        containerPath: /data
